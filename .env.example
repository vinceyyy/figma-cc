# Required: OpenAI API key for AI feedback
OPENAI_API_KEY=sk-your-key-here

# Optional: API key for access control
# When set, all API endpoints (except /health) require X-API-Key header.
# When empty or unset, auth is disabled (local dev mode).
# API_KEY=your-secret-key

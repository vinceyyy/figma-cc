# Required: OpenAI API key for design analysis
OPENAI_API_KEY=your-openai-api-key-here

# Optional: API key for endpoint authentication
# When set, all requests (except /health) must include X-API-Key header.
# When empty or unset, auth is disabled (convenient for local development).
# API_KEY=your-api-key-here

# Optional overrides
# MODEL_NAME=openai-responses:gpt-5       # default — best annotation accuracy
# MODEL_NAME=openai-responses:gpt-5-mini  # faster and cheaper, less precise annotations
# PERSONAS_DIR=./personas
# LOG_LEVEL=DEBUG
